<h1>Post publicados en el blog</h1>

<table>
  <thead>
    <th>Autor</th>
    <th>Título</th>
    <th>Contenido</th>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.author %></td>
        <td><%= post.title %></td>
        <td><%= post.content %></td>
      </tr>
    <% end %>
  </tbody>

</table>


<%= link_to 'Nuevo Post', new_post_path %>
